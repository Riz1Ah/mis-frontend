import { useEffect, useState } from "react";
import loginService, { BasicAuth, LoginResponse } from "../services/login-service";
import { CanceledError } from "../services/api-client";

function useLogin(creds: BasicAuth) {
    

    
    // console.log(headers)
    // useEffect(() => {
    //     // setIsLoading(true);
        
    
        
    //   }, []);
    // return { sessionId, error };
}

export default useLogin;
